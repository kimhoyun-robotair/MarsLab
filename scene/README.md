# MarsLab Scene

`marslab_scene` is the offline scene construction package for MarsLab. Phase 1 provides the strict recipe, artifact, coordinate-frame, compatibility-policy, and layer data contracts used by later migration phases.
